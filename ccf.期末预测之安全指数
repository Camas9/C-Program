#include<stdio.h>
int main(){
    int n,i;
 int w,s;
 int sum=0;
 scanf("%d",&n);
 for(i=0;i<n;i++){
  scanf("%d %d",&w,&s);
        sum=sum+w*s;
 }
 if(sum<0){ 
  printf("%d\n",0);
 }
    else 
 printf("%d\n",sum);
 return 0;
}
